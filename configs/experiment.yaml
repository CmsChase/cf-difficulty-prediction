schema_version: 1

project:
  name: cf_difficulty_prediction
  random_seed: 42

features:
  include_points: true
  include_tags: true
  min_tag_frequency: 5

splits:
  contest_grouped:
    train_frac: 0.8
    valid_frac: 0.1
    test_frac: 0.1
  forward_time:
    train_frac: 0.7
    valid_frac: 0.1
    test_frac: 0.2
